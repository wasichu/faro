services:
  db:
    image: postgres:18
    container_name: faro_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: faro_dev
    ports:
      - "5432:5432"
    volumes:
      - faro_pg_data:/var/lib/postgresql

volumes:
  faro_pg_data:
